name: Check Container Build

on:
  push: # TODO: Remove before merge
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - run: docker run -v ${PWD}:/project -w /project devspacesh/devspace devspace build --skip-push --profile production

      #- uses: docker/setup-buildx-action@v1

      #- uses: docker/build-push-action@v2
      #with:
      #push: false
